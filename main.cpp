#include <iostream>
#include <string>

int main(){
    int cuccut {10};
    long long dicho {123};
    std::cout << cuccut << " và 0 " << dicho << std::endl;
    std::cout << sizeof(dicho) << " is the size of 'dicho' " << dicho << std::endl;
}